__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
# 分发包的**构建模板**要入库（构建产物 dist/ 本身仍忽略）
!tools/hand_3d_s80c/dist/
build/
data/recordings/
data/*.db
*.log
# 原生日志归档目录（运行期把 runtime 目录 rmtree 之前的 SLAM 原生日志留档到这里，
# 保留最近 20 份），连同 tools/dedup_gripper_native.py 的 JSON 报告一起**整体不入库**：
# 都是本机诊断产物，动辄几十 MB（实测 57MB/22 份）。`.log` 那行只挡住了日志本体，
# 报告是 .json 会漏网。
logs/
.venv/
venv/
venv_lite/
*.fifo
*.h5
*.mp4
.DS_Store
*.bak_*
*.corrupted_*

# 备份包/个人配置/生成数据
*.tar.gz
*.bak
*.old
*.parquet
.vscode/
.claude/
keypoints_output/
exports/
# 本地取回的录制素材与上色产物（双目原视频等，体积大且属外部数据）
rec001(1)/
tools/weights/
wheels/          # scripts/pack_wheels.py 生成的离线部署包（不入库，拷给客户用）
imgui.ini
.polyscope.ini
tools/stereo_s80m/offline_slam_output/
tools/stereo_s80m/hand_benchmark/
data/*.db-shm
data/*.db-wal
tools/demos/stereo_depth_demo/*.bin
tools/demos/stereo_depth_demo/*.png
tools/demos/stereo_depth_demo/build/

# 本地私有配置（模板见 *.example.json）
data/server_config.json
data/device_names.json
data/tasks.json

# 真机标定（本地提取产物，跑 tools/hand_3d_d435/tools/extract_d435_color_calib.py 再生）
tools/hand_3d_d435/calibration/
pipeline.db

# 手套工具包（第三方 SDK 本地参考，永远不上传）
stouch_glove_toolkit-Stouch_Glove_Toolkit-beta/

# 夹爪上位机程序及原生资源（同手套工具包策略，整个目录不上传）
online/
# 夹爪随包交付的原生资源（从 online/ 镜像而来：厂商 SDK/二进制/ORBvoc.txt 共 785MB，
# ORBvoc.txt 单文件 138MB 超 GitHub 100MB 限制，且为第三方二进制，不入库）
# 触觉 Sightac SDK 例外：core/gripper/sightac_sdk/ 已 pyarmor 加密，随包入库
core/gripper/native/
# 极简版发布目录/本机部署目录，整体不入库。
# 2026-09-16 起 start_lite.bat/.sh 与 使用说明_lite.md 三个分发壳住在**仓库根**
# （与 main_lite.py 同级，使根目录直接可运行）；lite_package/ 里那三份是
# scripts/pack_lite.py 收集时复制进去的产物，同样不入库——原先的 !负向规则
# 已随之删除，否则每次重打包都会留下未跟踪副本。
lite_package/*
