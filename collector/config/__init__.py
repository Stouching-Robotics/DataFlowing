"""config 包 —— 应用配置、国际化与版本号。

版本号集中定义于此：settings.APP_VERSION 及其他模块均从这里取值。
"""

__version__ = "1.2.1" # 修复大文件上传被 10 秒误杀（urllib3 发送请求体阶段 socket 超时错用连接超时，服务器慢读停顿超 10s 即 Connection aborted，现已改用读超时窗口）；上传对话框关闭后任务在后台继续完成（手动上传并入主窗口共享上传队列，重复提交自动跳过）。