"""config 包 —— 应用配置、国际化与版本号。

版本号集中定义于此：settings.APP_VERSION 及其他模块均从这里取值。
"""

__version__ = "1.3.5" # 夹爪扫描配对改走 ESP32 NVS 序列号 + camera-service 自愈（两个都是「不恢复」的毛病，不是「会坏」的毛病）。相机侧：UVC 相机（DECXIN 1bcf:2d4f + 两个 Sightac 0c45:636f）会**静默卡死且不需要任何进程碰它**，用户看到的「RGB 只剩第一帧」「camera-service 未在限定时间取得全部相机的有效 MJPG 帧」根子是**服务不会自愈**——实证 2026-09-15 右目 Sightac 是自己夜里坏的（21:53 还在正常推流，09:24 启动就坏，中间没有任何进程打开过它、内核无 usbfs/uvcvideo 消息，机器整夜没睡、14.9 万条内核日志里查不到肇事者）→ 别再去查「谁把它弄脏的」，服务必须能自愈。两种卡死要分开治：**停摆**（开着流突然不出帧、`received` 冻住、内核一条消息都没有，6~82 秒随机、与负载正相关）**重建 libusb 上下文就够**（实测 6/6、5/5 全恢复，USB 复位是白付的代价）；**开不起来**（`uvc_start_streaming failed: I/O error` / `UVC format/interface negotiation failed: Invalid mode` / `negotiated_payload` 在 3072↔0 之间跳）**重建上下文救不回来**（实测连试 9 次全败，设备固件侧卡死了），只有 USB 复位/重枚举能洗。改动：新增 3 秒停滞看门狗（`STREAM_STALL_TIMEOUT_SECONDS`，首帧 4 秒）——此前整路静默报废没有任何人管；停摆（`rc>0`）走 `uvc_exit`+`uvc_init` 重建上下文；开不起来（`rc<0`）计入 `hard_failures`，**连续 2 次**升级成 USB 复位（`USB_RESET_LIMIT` 3 次封顶——设备真坏了时不设上限会变成每 2 秒复位一次、把同 Hub 的另外两路一直按在地上），而停摆会**清零**该计数，免得几次停摆凑数误触发复位去打扰邻居；**复位要抢在 `uvc_stop_streaming` 之前**（`teardown_failed_open`）——卡死设备上 `uvc_stop_streaming` 会一直等它那批永远回不来的等时传输，实测能把客户端那 2 秒宽限耗光而被 SIGKILL，进程死在这中间、设备就停在激活的 alt setting 上，下一轮启动照样起不来（用户 09:24 的日志正是断在 startup failed 与 usb reset 之间）；所有阈值由客户端窗口倒推（`SERVICE_START_TIMEOUT_S = 10.0`：每次失败约 0.3 秒 + 1 秒间隔，第 2 次失败在 t≈2 秒触发复位、重枚举加重开约 1 秒、t≈3 秒就能出帧，10 秒够用；旧的「重开 20 秒零帧才复位」在真实启动场景**永远够不到**——20 > 10，且只挂在零帧分支上，实测到的 `I/O error` 根本不走那条）；复位语义实测——打在**健康**设备上是原地端口复位（内核 `reset high-speed USB device number N`，**devnum 不变**），打在**卡死**设备上是掉线再重枚举（`USB disconnect` + `new high-speed USB device`，**devnum 变了**）**这才会好**，而 libusb 在掉线重枚举的窗口里返回 `rc=-4 (No such device)` 是**报错但事办了**、别被它误导；`libusb_reset_device` 用 `dlsym(RTLD_DEFAULT, …)` 取，不新增 NEEDED、不需要 root（设备节点 udev MODE=0666）；顺带修每秒 fps 上报基线（`previous_received` 从 0 起算，重开后把进程启动至今的全部帧数当成本窗口增量，算出 164/207/251 的假速率——纯日志问题，但会把人带偏）。真机验证：确定性升级测试（第 1 次失败不复位、第 2 次 `usb reset: rc=0 (Success)`、打满 3 次后停手）、三路 90 秒回归 0 停摆 0 误复位（2694/2683/2684 帧 ≈29.9fps）、SIGTERM 退出 1.16 秒（旧 1.13 秒）不超客户端 2 秒宽限。**源码位置**：`online/` 在 .gitignore:56 里、不入库；本次把这两个程序的源码连同构建配方搬进了 `core/gripper/camera_service_src/`（`./build.sh` 是唯一构建入口——它替我们处理 libusb 的 pkg-config shim、`-DCMAKE_POLICY_VERSION_MINIMUM=3.5`、以及链接后去掉 RUNPATH 里那个空条目，手工 cmake 会在这些地方卡住；产物装到 `core/gripper/native/camera_service/build/`，旧件按 `.pre_rebuild_<时间戳>` 备份），以后不必再从 libusb 头文件、pkg-config、RUNPATH 那一整串摸起。交付副本 `programs/camera_service/src/camera_service.c` 未动、与 `程序清单.txt` 第 18 行一致。**RGB 断联的根因（2026-09-15 查明，本条是用户「还是会断联」的正面回答）：服务自愈之后相机仍会断，断的原因是 DECXIN 跑在高带宽等时档 alt7 上时每 68~116 秒静默停摆一次，两块控制器（AMD `1022:43fc`/PCI `0000:0a:00.0` 与 `1022:15b7`/`0000:74:00.4`）上都一样。机制**未查明**：停摆当时实际吞吐只有 1.66 MB/s（峰值 2.63），离 alt7 预留的 10.24 MB/s 差得远，「带宽不够」解释不通——**别把机制写死**。alt6 同样三路下长跑不停，而两档出帧率一模一样（都 30.00 fps），alt7 多出来的每帧余量（327680 对 241664 字节）换不到任何看得见的好处。此前两个结论已推翻、别再引用：「bus 1 上 alt7 是好的」——那条所谓干净腿根本没跑在 alt7 上，旧二进制里 ini 的 `forced_*` 是死的、服务只把 ini 的值原样打进日志，读到的 `alt=7 payload=1280` 是它在**复读**，**判断一条腿真实档位只认 `[LIBUVC-QUIRK] ... alt=N payload=M ... xfers=` 那一行**；以及同源的「停摆是控制器特异的」。改动：libuvc 新增运行时接口 `uvc_set_altsetting_override()`（声明在 `include/libuvc/libuvc.h`、实现在 `src/device.c`，`uvc_device_handle_t` 是 calloc 出来的、新字段自动零初始化），`forced_altsetting`/`forced_payload` 从此**真正生效**——此前这两个 ini 字段只用于校验和打印，改 ini 完全不改变行为，极容易误判；起始档位由 app 决定、取**档位表里最保守的一档**（`uvc_camera_service.py` 的 `_starting_mode` 优先级：上一轮学到的 > 最后一档 > 扫描程序报的首选档），**不按机器/控制器写死**（机架插在哪条总线不固定，用户明说不是永久的），曾经那张「敌意控制器」表已删；降档梯子 `maybe_downgrade_altsetting`：当前档位连着停摆 2 次、或连着开不起来 3 次（后者覆盖 STREAMON 阶段就排不下带宽、流根本起不来的情形，停摆那条路永远等不到）就退一档并把结论**落盘**（键 = `vid:pid × 控制器 PCI 路径 × 相机序列号`，一个键一个文件，放 `native/gripper_version1/camera_modes/`——**不能放服务的 runtime 目录，那里每次租约释放整个删掉**），稳定跑满 `CLEAN_RUN_SECONDS=300` 清零停摆计数；**降档必须同时清掉两个计数**——门槛是拿 `==` 比的，只清一个的话另一条路降完一档就永远不再命中，表里剩下的档位形同不存在（DECXIN 只有两档，正好把这个缺陷盖住了）；两条路的日志都在调用**之前**抓计数，否则清零后只能打出「0 停摆」，正好把最该看的那次降档说成没有依据；`tools/discover_uvc_config.c` 如实报**相机描述符里的首选档**（alt7），app 拿它跟服务端档位表对一遍、确认这一对是梯子上的任意一档，对不上就报「扫描程序与服务端档位表已经脱节」（那说明两个程序分开编了，必须一起重编）；`./build.sh --test` 从点名跑 `test-frame-integrity` 改成走 `ctest`——`tests/test_altsetting_ladder.c` 虽然在 `CMakeLists.txt` 里注册了 `add_test`，此前却一次也没被执行过，上面那个「降一档就卡死」正好落在它本该覆盖的范围内。真机验证：ini 强制 alt7 → 78 秒停摆 → 116 秒再停 → 服务打出 `2 stalls at alt=7; dropping to alt=6 payload=944` → 重开在 alt6 → 169 秒 30fps 零停摆，整条降档+恢复路径走通；装 app 自己生成的 ini（alt6 默认）跑 **600 秒三路：0 停摆 / 0 重建 / 0 启动失败 / 0 复位**，17513/17496/17487 帧（30.00/30.00/29.80 fps），日志里 `[LIBUVC-QUIRK] … alt=6 payload=944 endpoint=944 xfers=32 (altsetting override)` 证明确实跑在 alt6 上、ini 真的生效了；装机后再做一次 300 秒回归同样全零停摆；装机产物与源码构建逐个 md5 相同、RUNPATH 仍只有 `$ORIGIN/third_party/libuvc`。离线侧新增 `tools/tests/test_camera_altsetting_mode.py`（19 条，覆盖最保守档/学到值优先/表外值忽略/扫描程序与服务端脱节必须先报错，并钉死与 C 测试共用的落盘键串），`test_altsetting_ladder.c` 补上「两个计数都要清零」的断言并**反向验证过不是空跑的**（把清零那行注释掉，该测试当场断言中止）。装机后需重启主程序才能同时拿到新的 `uvc_camera_service.py` 与新服务，否则两边档位要求对不上会像 2026-09-15 09:24 那样整个起不来。扫描侧：移植交付包「设备扫描逻辑与程序_20260914」，夹爪与 UVC/ESP32 的配对从 **USB 根端口/控制器**改走 **NVS 序列号**——旧规则（`_bus_root`/`_root_port`，外加「同一 USB2 总线出现双 rig 就拒扫」）的前提就是错的：Fays 是 USB3/FT602 挂在配套的 5000M 总线上，跟 UVC/ESP32 本来就不共根，只能靠猜。新链路：UVC 组 → 唯一 ESP32（`bus-outer.` 开头）→ 串口发 `QF` 读它 NVS 里绑定的 Fays 序列号 → 与官方 SDK 枚举出的 `device.serial` 比对（`_resolve_fays_group` 三步，任一步不唯一就明确拒绝；`_match_esp` 拒收空序列号，不再按端口或枚举顺序猜）。SDK 探测前先 `validate_fays_superspeed` 预检，再以 `serial_only_fast=True` 快速枚举序列号（`fays_serial_probe.py` 加 `--serial-only-fast`；超时不再只报一句 timeout，解码 stdout/stderr 并附最后 20 行），`_probe_serial` 交叉核对 SDK serial 与 NVS 绑定；双 rig 新增「在用时跳过」——`fays_device_guard(port, timeout=0.0)` 探到另一 rig 正持有该设备就跳过而不是占用。串口侧：`CONNECT_BOOT_DELAY = 2.0` 死等改成 `CONNECT_BOOT_TIMEOUT = 2.0` 轮询 `?` 握手（实际尝试次数按真实值上报，不再报上限），打开加 `exclusive=True`，新增 `query_fays_serial()`/`set_fays_serial()`（`QF` 查询 / `WF:<serial>` 写入，值校验 `[A-Za-z0-9._-]{1,64}`）；顺带修 `GripperSerial.send()` 只拿首字符去查命令表（`[:1]`）——`"QF"` 因此永远匹配不上自己的期望表，改成先查全命令再退回首字符。UVC 发现程序侧：`_native_env(binary)` 改成优先用二进制自带的 `third_party/libuvc`（不再指望环境里恰好有），`_run_discovery` 加互斥的 `--topology-only`/`--identity-only`/`--skip-flash` 三种扫描深度。**前置条件：ESP32 固件必须认识 `QF`/`WF:`，当前 `online/esp32_proc` 的固件还不认识，会失败关闭并报「查询 Fays 序列号失败: ERR」，需先烧固件。** 新增离线自检 `tools/tests/test_gripper_fays_pairing.py`（假串口 + 假桥接 + 假探针，覆盖 QF/WF 往返、ESP 匹配、按序列号解析、整套 acquire、UVC 关联，并核对部署的原生程序确实内嵌了新参数）。s80c 上色工具修隔帧重复（`tools/s80c_arm_convert/convert_arm_dataset.py`，v1.3.4 之后单独提交的补丁）：`pooled_episodes_v1` 这版录制**每帧写两行**（`frame_index` = 0,0,1,1,…，同一帧两行的 `timestamp`/`hardware_ns` 逐值相同）而视频只有一帧，工具把行数当帧数（`n = tbl.num_rows`）、主流程再按视频帧号 1:1 取 `hw_ns[i]`，于是每个时间戳被连送两遍——离线 ISP 对重复时间戳复用上一帧的输出，输出视频就隔帧重复。实测 rec001 无损版 833 帧里 394 帧与前一帧逐字节相同、且 394 个全落在奇数位（每对时间戳的第二个），源视频 0 重复；同一根因还让 IMU 侧车**少一半**（修复前 `hw_ns[:n_frames]` 只够到第 417 个时间戳，其后的 IMU 样本全被「晚于末帧 hw」规则丢掉，输出只剩 416 行 / 13.8 秒，修复后 832 行 / 27.7 秒，与视频时长 27.8 秒对上）。现按 `frame_index` 折叠冗余行，但**只在同一 `frame_index` 名下所有行的 `timestamp` 与 `hardware_ns` 逐值相同时才折**，否则打告警原样不动——全库 142 个带 `frame_index` 的 parquet 里 129 个本就每帧一行（空操作）、1 个真冗余（rec001，1666 行 → 833 帧）、12 个行间时间戳互不相同（折叠会丢时间戳，一律只告警）。上色链路一行未动，同一份 config、同一个离线 ISP、`--sdk-wb-auto` 照旧，灰世界增益与修复前逐位相同。承接 v1.3.4 的新夹爪接入即自动生成运行标定并连接（Fays S80M）：此前接入一只新夹爪要人工两步——先在夹爪上位机点 `device_setup`、再在采集机跑 `tools/import_gripper_calibration.py` 导入，漏掉任一步主程序直接拒绝启动（「当前 Fays 缺少运行标定文件…请先在夹爪上位机中运行 device_setup」）；现在 `SingleFaysLease.acquire()` 发现 per-serial YAML 缺失时，就在设备锁内调厂商导出程序从**这只夹爪自身**读出厂标定、生成 SDK YAML 与 ORB YAML（鱼眼 `stereoRectify` 求 P1/R1 + IMU 外参 `T_b_c1` + 探针读五项噪声/频率），复核落盘路径后直接连上，已有标定走 `isfile` 快路径（启动零额外开销、不碰设备）——这两个文件是**逐设备**出厂标定不可跨设备复用（实测 098/099 三组全不同），只能现场读；设备面板夹爪条目右键加「重新读取出厂标定」（录制中禁用，标定前自动关该夹爪、完成后开回），`tools/import_gripper_calibration.py` 加 `--generate`（旧文件先备份 `.bak_<时间戳>`）供命令行强制重生成；顺带修掉生成产物第 3 行的出处注释——厂商模板写死指向 `3500000261870088`，照抄会让每只新夹爪的 IMU 外参都被记成「取自另一台设备」，现改写成本机序列号（历史产物不动）。承接 v1.3.3 的力矩阵/触觉力与 RGB 视频的时间戳对齐：**力侧此前完全没有时间戳**——同一行里的 RGB 帧和力样本不是同时刻采集的，RGB 走外部帧源队列取队头最旧帧、力/矩阵走 latest-wins 单槽取当下最新，于是「行号相同」≠「时刻相同」，力恒领先 RGB 且领先量随录制时长增长（RGB 源 30.84fps vs 写线程 30fps，实测约 1 帧/秒，下游观测到的滞后 3 帧就是这么来的），力矩阵走独立泵线程还与 3 向量力各记各的时刻。修法是让每行自描述：`tactile_ready` 加第 5 个载荷 `capture_ns`（在 SDK 回调入口取），`pipeline.write_tactile_force/_force_matrix` 落成独立的 `observation.gripper_{side}_force_ns` / `_force_matrix_ns` 稀疏 int64 列（与 `hardware_ns` 同宿主单调钟时基，features 里声明 `encoding: host_monotonic_ns`），下游按时间取最近邻即可还原真实配对，精度上界=半个力样本间隔；顺带修 `rgb_frame_ready`/`stereo_frame_ready` 的 `pyqtSignal(int)` 把 `time.monotonic_ns()` 按 C++ qint32 静默截断成负数（每 2.147s 翻符号），改 `object` 封送——这也解释了此前用 `hardware_ns` 解回卷时凭空造出上百帧假漂移、把"中位 5.8 帧 / 最大 ±80 帧"报成对齐误差（真值是 ~3 帧，正确规则是**无符号**：单调钟 + FIFO 消费 ⇒ 真步长恒非负）；新增 `scripts/align_modalities.py` 处理端最近邻对齐工具，对 v1.3.3 之前录的 episode 明确报「无法对齐（只能重录）」而不用增长率去猜。承接 v1.3.2 的 SLAM 陈旧帧入库前拦截与轨迹显示深拷贝修复。：桥接按「连续回退次数」判定——单帧脏读丢弃、连丢 30 帧才判定时钟真跳变并换基准放行（此前每一帧回退都会让核心库清空 IMU 队并重建地图、位姿停发 0.2–1.34 s，静止时更久），新增 `[TIME_DROP]`/`[TIME_REBASE]` 现场行并**无条件**打进 GUI 日志（此前落进未匹配行被 50 条上限吃掉——实测原生日志 4 次回退、GUI 日志 0 条，真出事时反而看不见），且带 `seq`/`prev_seq` 对比结论以区分「SDK 乱序投递 / 重复投递同一帧 / 帧配了旧时间戳」三种成因（时间戳上三者长得一模一样，对策完全不同）；逐秒遥测 `[FPS_DATA]` 改「只在异常时出声」（健康静默、进入异常与恢复各一行、持续异常每 30 s 重复，全量逐秒流仍在原生日志里），并修 `wait_sdk_ready` 就绪门被**粘滞** `state.error` 锁死——任何信息性输出（如 ld.so 的 `cannot be preloaded ... ignored.`）都会让标定明明已完成的这一次返回 False、报出误导的「未等到 [FAYS-CALIB] 标记」（2026-09-10 18:32 假超时事故，文案白名单 + 去掉粘滞判断双管修）；原生日志在 runtime 目录 `rmtree` 之前留档到 `logs/slam_native/`（保留最近 20 份），修「一次干净退出的会话其原生日志不可恢复」——排查 SLAM 崩溃时唯一的证据曾随目录一起被删；轨迹显示去掉 Qt 主线程上的整条深拷贝（正是这次拷贝让 `_rendered_trajectory is traj` 判等恒为假、每个位姿都在主线程复制随会话线性增长的点列，即「画一段时间后帧率变低」的直接原因），显示上限 400→6000，并以向量化投影 + 一次 `drawPolyline` 绘制，修「十几秒后轨迹显出折角」（降采样按 stride 翻倍使顶点数恒落 [上限/2, 上限]，上限 400 时每段跨 67 ms 且随 stride 每翻一倍继续变粗，观感像 SLAM 出点变慢而数据一直满 30 Hz，是绘制侧丢的）；RGB 外部队列在录制开始/结束/中止三个边界排空，修「上一段残留 + 本段启动窗口累积被写成新视频开头」（实测每段开头约 28 帧与上一段 episode 末帧逐帧匹配，队列满时还占 ~110 MB/槽）；任务进度只上报后端当前任务列表里存在的任务（本地自建任务平台没这个项目，上报必被拒 400），且后端该项目口径为 `"sessions"`（从没收到过任何上报）时首次送本机**全量**当基线——只送增量会把后端计数从 session 数砸成增量本身（现场：3/3/3 的项目再录一条变 1）。承接 v1.3.1 的力矩阵 5 档落盘与 SLAM 停落 slam_pose 列。
