"""config 包 —— 应用配置、国际化与版本号。

版本号集中定义于此：settings.APP_VERSION 及其他模块均从这里取值。
"""

__version__ = "1.3.1" # 力矩阵落盘规格改为 5 档可选（int16 / int16×10 / ×100 / ×1000 / float32，工具栏「🎚 力矩阵精度」可切，默认 ×100 —— 误差降到传感器自身噪声的 0.9% 而体积仍是 float32 的 38%），倍率改记在**每段一份**的 meta/episodes 行（新增第 11 列 force_matrix_specs，修「同任务换档时前一段被后一段顶掉、回放与训练按面值读放大 10/100/1000 倍的数」的静默错），并新增 scripts/repair_force_matrix_scale.py 按实测比值给已录段补写倍率（只补 meta 行、不碰 data 字节）；触觉热力图显示量程改「下限固定 + 慢速自适应」（τ≈8s），修固定量程下量化把梯度压平、用力大小只剩接触面积在变；任务进度上报 404 后带 10 分钟冷却期重探，服务端补上端点后采集端无需重启自动恢复；SLAM 停落 slam_pose 列（定长 7 值列填不满 20/20/60ms 的位姿突发，实测 26.5% 的行被填成 [0]*7 会被下游当真实位姿读），slam_trajectory 成为位姿唯一落盘形态。承接 v1.3.0 的 UMI 夹爪全链接入（Fays S80M 双目 SLAM 位姿/轨迹 + Sightac 左右触觉力与 250×250 力矩阵 + DECXIN RGB；面板夹爪分组、资源缺失自动隐藏），支持双臂双夹爪同录（rig1 旧槽位/数据列契约不变，rig2 加 gripper_2_ 前缀；两 rig 独立 CPU 亲和分区零共享物理核 + raw 流专用核 + 30fps 空桶看门狗）；SLAM 位姿坐标约定按新 ORB 核修正（X=右/Y=正对/Z=上，原点后姿态相对化，起始不跳变）；左目显示稳 15fps（奇偶抽帧）且 raw 流断线自动重连；轨迹并入 episode parquet 列不再落 txt 侧车；夹爪脱离 online/ 自持（原生资源镜像 core/gripper/native/ 不入库、Sightac SDK pyarmor 加密随包、新增 tools/import_gripper_calibration.py 接入新夹爪标定）；多台 S80M 按序列号/USB 拓扑路径区分（--device-serial/--device-path）。