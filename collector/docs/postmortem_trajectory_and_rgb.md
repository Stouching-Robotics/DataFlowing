# 复盘：轨迹「有棱有角」、RGB 段首旧帧与帧空洞

2026-09-11（一、二、三节）；2026-09-18 续写第四节（RGB 帧空洞，v1.3.10）

前两个问题都是在「录下来/显示出来的东西不对」这一层被发现的，而且**都被先怀疑
成数据生成侧的故障**（SLAM 出点变慢、相机多投了帧）。实测结论都是：数据本身
没问题，问题出在**消费侧的一处边界处理**。

| | 轨迹有棱有角 | RGB 段首旧帧 |
|---|---|---|
| 症状 | 画到约 20 s 后折线开始切角 | 新 mp4 开头几十帧是录制前的画面 |
| 直观误判 | SLAM 出点频率下降 / 缺帧 | 相机多投了帧 / 编码器预热 |
| 真实根因 | 显示侧降采样上限 400 | 外部队列从不在录制边界清空 |
| 数据本身 | 完好，一直满 30 Hz | 完好，但段首约 1 s **内容错位** |
| 修复位置 | `ui/pose_view_qt.py` | `core/pipeline.py` |
| 回归防护 | `tools/tests/test_pose_view_qt.py` | `tools/tests/test_rgb_prestart_frames.py` |

---

## 一、轨迹「画了 20 秒后变得有棱有角」

### 1.1 现象

录制过程中，位姿视图里的轨迹线一开始是光滑的，画到大约 20 秒后开始「有棱有
角」，观感像 SLAM 出点频率掉了下来。

这个问题是**紧接在另一个修复之后**被提出来的：前一轮刚修完「帧时间戳回退 →
核心库清 IMU 队列 + 重建地图 → 位姿停发」，用户的原话是

> 你只是把陈旧帧丢了，没有解决我因此缺帧的问题

也就是怀疑棱角 = 之前丢帧的余波。这条怀疑是错的，但它把排查方向钉在了
「到底缺没缺帧」上，反而是破案的关键。

### 1.2 排查：先钉死是哪一侧

第一步不是看绘制代码，而是**验证数据有没有缺**。

把 episode parquet 里的 `slam_trajectory` 列取出来（扁平的
`[t,x,y,z,qx,qy,qz,qw]×n`，不是 3 元组列表），量相邻位姿点的时间间隔：

```
间隔恒为 33.34 ms  →  整段录制一个点都没缺，一直是满 30 Hz
```

数据是满的。**缺帧假设当场排除**，问题只可能在绘制侧。于是转向
`ui/pose_view_qt.py`，很快看到 `_decimated_trajectory`。

### 1.3 根因

`ui/pose_view_qt.py:277` 的 `_decimated_trajectory` 按 **2 的幂**翻倍 stride：

```python
while length > DISPLAY_TRAJECTORY_MAX_POINTS * self._display_stride:
    self._display_stride *= 2
```

这个写法的后果是——**画出的顶点数永远落在 `[cap/2, cap]`，永远不会超过上限**。
上限是 400 时：

| 真实点数 | stride | 画出顶点 | 每段跨度 |
|---|---|---|---|
| 400 | 1 | 400 | 33 ms |
| 401 | 2 | **201** | **67 ms** |
| 801 | 4 | 201 | 133 ms |
| 1601 | 8 | 201 | 267 ms |

30 Hz 下 400 点 = **13.3 s** 就越界，顶点数从 400 腰斩到 201，折线每一段的
跨度直接翻倍——这就是「突然变有棱有角」的那一刻。加上 SLAM 双目 + IMU 初始化
要 ~6 s 才开始出点，`13.3 + 6 ≈ 20 s`，与用户说的「20 秒」吻合。

关键判据是**转角**，不是「走样偏离」：在同一段弧长上，折线的转角随每段时长单调
增大，这才是眼睛看到的「棱角」。真实录制轨迹（334 点 / 11 s / 对角 563 mm，
视图 680×420）实测：

| stride | 每段 | 段中位位移 | **顶点处转角中位** |
|---|---|---|---|
| 1 | 33 ms | 0.66 px | **11.09°** |
| 2 | 67 ms | 1.37 px | **17.37°** |
| 4 | 133 ms | 2.44 px | **22.04°** |
| 8 | 267 ms | 3.95 px | **48.91°** |

（走样偏离也量了，stride 2/4/8 对应最大 1.28 / 4.18 / 11.80 mm——偏离其实不大，
单看这个指标会低估问题，所以判据要取转角。）

### 1.4 评估过的方案

**① 改 `_decimated_trajectory` 用 numpy 切片抽点** —— 否决。
实测比现有的索引列表版**更慢**：`np.asarray(24000 个 tuple)` 要 1.75 ms，
而现有实现只要 0.54 ms。因为轨迹点是 Python tuple 序列，转 ndarray 的开销
超过了切片省下的。**不动这个函数。**

**② 只把上限调大，绘制代码不改** —— 不行。
旧实现是逐点 `_project` + 逐段 `drawLine`，在 6000 点上要 **~13 ms/帧**。
上限之所以一直压在 400，根源就在这个逐点 Python 开销——它是个自我保护。
不批量化就不能提上限。

**③ 最终采用：提上限 + 批量化，两件必须一起做。**

### 1.5 最终方案

1. **上限 400 → 6000**（`ui/pose_view_qt.py:27` 的 `DISPLAY_TRAJECTORY_MAX_POINTS`）
   越界点从 13.3 s 推到 200 s，正常录制再也碰不到。
2. **新增 `_project_many`**（`ui/pose_view_qt.py:331`）——一次批量投影成
   `QPolygonF`，与逐点 `_project` **逐点等价**（有测试断言 < 1e-9 偏差）。
   `_refresh_cached` 改调它。
3. **`_draw_static` 改为一次 `painter.drawPolyline`**（`ui/pose_view_qt.py:377`），
   不再逐段 `drawLine`。

6000 这个值是实测选出来的：它落在「顶点下限 3000 已让折线远密于像素」与
「成本 ≤ 6 ms/帧」的交点上。

### 1.6 代价（实测，有界）

`_refresh_cached` + 整帧 `render` 的耗时：

| 轨迹点数 | 实际画出顶点 | 刷新 | 绘制 | 合计 |
|---|---|---|---|---|
| 6000 | 6001 | 4.20 ms | 1.84 ms | ~6.0 ms |
| 12000 | 6001 | 4.20 ms | 1.82 ms | ~6.0 ms |
| 24000 | 6001 | 4.25 ms | 1.84 ms | ~6.1 ms |

**代价不随会话长度增长**——因为顶点数被上限钳在 6001。配合 25 Hz 刷新 +
30 Hz 重绘，约合单核 14%。主要开销是 `_project_many`（6001 点 3.55 ms）。

### 1.7 回归防护

`tools/tests/test_pose_view_qt.py` 新增两个检查：

- `check_decimation()` —— 对 n ∈ (cap/2, cap, cap+1, 2cap, 4cap) 断言画出顶点数
  ≥ cap/2；并单独钉死 `drawn_vertices(401) >= 400`（旧行为在这里会塌成 201）。
  **已做反向验证**：把上限改回 400 后该断言必然失败。
- `check_projection_equivalence()` —— `_project_many` 与逐点 `_project` 逐点一致。

### 附：同链路上的两个相邻问题（易与本题混淆）

- **轨迹帧率随会话下降**：`update_pose` 在 25 Hz 时间闸门**之前**深拷贝整条轨迹
  `tuple(tuple(p) for p in trajectory)`，让下游 `trajectory is self._rendered_trajectory`
  的判等**恒为假**——而桥接侧 `bridge.py` 的契约正是「新点才重建不可变元组，UI 按
  对象同一性省去无变化重绘」。去掉那次拷贝即可。检测器：
  `widget._rendered_trajectory is traj`（改前必失败）。
- **陈旧帧 → 时间戳回退 → 地图重建 → 位姿停发**：修在**桥接入库处**
  （`core/gripper/orb_slam_src/ORB-SLAM/Examples/fays/TimeRegressionGuard.h`，
  2026-09-17 前在 `online/ORB-SLAM/Examples/fays/`），不能修进核心库——
  库内跳过陈旧帧会让 `GrabImageStereo` 返回未设置的单位位姿而 `mTrackingState`
  仍是 OK，应用侧的 `allFinite()` 判真，会把单位位姿当有效位姿发布，比不修更糟。
- **上面那条的判据：2026-09-17 已用真机取证判成 H2（重投递）**。现场 508 个
  `[TIME_DROP]` 里 seq **全部严格前进**，据此我曾断言「帧是新的、只有戳错」——那是
  循环论证：`seq` 若是 SDK 在宿主侧按投递次数自增的计数器（`fays_atrak_types.h` 对
  它没有任何语义说明），「同一帧被重投递」会给出**一模一样**的日志。两条假设在
  ts/seq 上处处等价（连 L 直方图都一样，L=4 占 84%），**差别只在像素**。判据只能用
  像素：桥接自带的取证开关（`KSQ_FAYS_DEBUG_DIR` + `KSQ_FAYS_DEBUG_SECONDS`(≤300) +
  `KSQ_FAYS_DEBUG_CPUS`，在回调**最前面**开录，早于 3-取-5 抽帧和单调守卫，需 >2GiB
  磁盘、约 26MB/s）抓每帧原始载荷，再跑 `tools/diag_frame_trace.py analyze <目录>`。
  **结果（09-17 170654 那次 86s 满载取证，5 个下陷帧、L 全是 4）**：5 个全判
  **重投递 H2**，没有一个逐字节相同 —— 旧数据在输出级之前**又过了一遍**（像素值被非
  均匀地改过：对往前第 L 帧的残差 2.1/4.7/10.8/12.5/16.9，正常相邻帧的噪声地板
  4.09；而整幅电平的最佳偏移 δ\*=0 ⇒ 这几次 AE 没动，不是「增益换了」）。判据是
  **边缘图**（`|Δx|+|Δy|`，加性电平在梯度里抵消）
  与**同窗正常帧对照**的比值：`e(L)/e(1)` = 0.58/0.32/0.74/0.54/0.57，对照
  1.00~1.18 ⇒ 结构确实贴着往前第 4 帧（且 argmin 全是 4）。**只用裸 mad 判不出来**
  —— 4 个在动的事件里 3 个的 `mad(i,i−L)` 落在正常帧带内，我的第一版判据（比 mad
  与全场基线的落点）因此给过 2 个**错误**的「只戳错」。取证时**必须让夹爪持续运动**，
  静止时相邻帧本来就几乎一样、判别力归零（#72 那次对照只有 1.00，余量最薄）。
  离线自检 16 项（含「旧判据在新模型上确实会误判」的反向验证）：
  `tools/tests/test_diag_frame_trace.py`。
  **结论**：丢帧的处置是对的，**不该**把戳修回来 —— 修回来等于把 80ms 前的
  measurement 当成当前帧喂进跟踪器；同时确认这批事件**丢的是一帧真的重复信息**，
  不是把唯一的新观测扔了。附带查到：设备戳是**合成格子**——差值恒为
  20ms 整数倍（09-16 那场 239907 行轨迹全部落在 20ms 整数倍上；09-17 真机取证
  4318 帧抽样 1043 帧，`ts % 20ms` 是同一个 **6.05ms** 相位，相邻差**全是** 20ms
  整数倍。轨迹里看着对齐是因为写的是 `ts − origin_ts`），而同一条流里 **IMU 戳是
  10µs 分辨率**（步进只有 1.000/0.990 ms 两档）——两个时钟粒度差 2000 倍。所以双目
  戳里没有比「第几帧」更细的信息，这正是回退量恒为帧周期整数倍、也是戳本身分不出
  载荷新旧的物理根因。
- **取证钩子在守卫的上游，这个位置差可以当判据用**：`g_debug_capture.stereo()` 在
  `stereoCallback` 内、`g_img_queue` 入队之前（`fayssense_orb_slam.cc:1572`），而
  `[TIME_DROP]` 在队列 pop + 3-取-5 之后（`:1806`）。队列是 FIFO、只排队不重排，
  所以**取证看到的戳就是设备给的戳**；又因守卫只看 3-取-5 的 3 帧，**取证下陷数
  应约为日志 TIME_DROP 的 5/3**。若某次取证「日志有、取证 0」，说明回退不是设备
  原始流带来的、我读错了字段，那次判读直接作废（工具会打这句警告）。
- **空连抓不到这个毛病**：丢帧率是机器负载的函数（09-17 实测 ≤5.5 → 0/秒；
  10.1~11.9 → 0.03~0.17/秒；**16.7~16.8 → 0.43~0.61/秒**，与 09-16 那条
  「机器被别人占满」同因）。09-17 16:49/16:52 两次空连取证（86/90 s）**0 下陷帧**、
  与同会话日志 0 条 TIME_DROP 吻合，基线 lag=1 中位 10.72 灰度级说明场景在动
  ——**这是没样本，不是判读结论**。要抓就得在负载高或真在录制时抓，且用
  `--seconds 300`（基线率下 120 s 只期望 ~4 个事件）。
- **取证目录重连即覆盖**：分片号恒从 `samples-0000.bin` 起编，同一个
  `KSQ_FAYS_DEBUG_DIR` 里第二次连接会从头覆盖第一次（09-17 16:49 那次 83 s 就这么
  没了；磁盘 5 片合计 2,222,220,384 B 与 `capture.json` 逐字节相等 ⇒ 只剩第二次那次）。

---

## 二、RGB 视频段首混入「录制开始前」的帧

### 2.1 现象

录下的 RGB 视频，开头若干帧是**录制开始之前**的画面。复现样本：
`data/recordings/UMIGripper_Action_AI/videos/chunk-000/gripper_rgb/episode-010.mp4`。
且是「有时候」——并非每段都明显。

### 2.2 排查：四重取证

**① 先排除「多写了帧」** —— mp4 帧数 == parquet 行数。
数量对得上，所以不是写多了，是**内容错位**。

**② 逐帧差分找内容跳变点** —— 每段开头约 **27~28 帧**之后内容突跳，
这个索引与 `Queue(maxsize=30)` 高度吻合。

**③ 跨段帧内容匹配（决定性证据）** —— 新段开头帧与**上一段的末帧**逐帧匹配：

| 新段 | 开头帧最匹配 |
|---|---|
| episode-026 | episode-025 **末帧** |
| episode-025 | episode-024 **末帧** |
| episode-019 | episode-018 **末帧附近** |

跳变之后匹配质量骤降（相似分 ~33 → ~41）。也就是说新视频开头那几十帧，
**物理上就是上一段录制的最后几十帧**。

**④ 段间时间戳间隙** —— 跳变幅度 ≈ 段间 wall_time 间隙 mod 4.295 s
（ep16 1.752/1.737、ep19 3.615/3.580、ep23 3.048/3.023、ep26 4.030/4.031……）。
`hardware_ns` 不是相机硬件时间戳，而是 `time.monotonic_ns()` 截断成 32 位**有
符号**，每 4.295 s 回绕一次——量步长必须用 `(hw[i+1]-hw[i]) mod 2**32`。

**⑤ 日志交叉验证** —— `logs/main.log` 里每段都有
`[录制] 丢帧统计: {'ext:gripper_rgb': N}`（N = 1~13），说明队列在**录制期间
也一直贴着 maxsize**，不只是开头。

### 2.3 根因

`CameraPipeline._start_async` 有一段清空逻辑，注释写着「清空录制前残留的旧帧和
传感器数据」——它排空了 `CameraSlot.frame_queue` 与 `_sensor_queue`，
**唯独漏了 `_external_queues`**；`finish_recording` / `abort_recording` 也都不排空。

而外部队列的**生产侧从设备打开起就一直在投递**（夹爪 RGB 由 bridge 常驻线程
`_rgb_run` 推送），只有录制期间才被 `_write_loop` 消费（每 tick 取 1 帧，不排空）。
于是两类「录制开始前」的帧被原样写成新 mp4 的开头：

1. **上一段停止时没被消费掉的残留** —— 深缓冲 `Queue(maxsize=30)`，1280×960×3
   的帧每张 3.52 MB，满队列 = 30 帧 ≈ **1 s / ~110 MB**。
2. **本段 `_recording=True` 之后、写线程就绪之前累积的** —— `_recording` 是
   **同步**置位的，而写线程要等 `start_episode`（编码器探测 + 建目录，几百 ms）
   跑完才启动，这几百 ms 内外部帧照投不误。

**为什么是「有时候」**：残留量**棘轮式累积**。早期段的跳变索引只有 4/2/5/8 帧，
后期稳定饱和在 27~28——因为生产者速率略高于消费者（见 2.7），队列只涨不跌。

**影响面不止夹爪 RGB**：`ui/main_window.py:970` 的 S80M 双目、`:1170` 的另一个
外部 RGB、`lite_window.py:709/772` 同理，全都是 external source，一并中招。

### 2.4 评估过的方案

**① 在 writer 侧丢弃每段前 N 帧** —— 否决。
`core/egodata_writer.py` 写第 0 帧前没有任何 warmup/丢前 N 帧逻辑，加一个看似
简单，但 N 不固定（随队列残留量在 0~30 之间浮动），N 取大了会误伤正常帧、取小了
治不了本；而且它把「边界语义」藏进了写盘层，真正的问题（队列没清）还在。

**② 在 `_on_gripper_rgb` / bridge 侧做门控**（录制未就绪就不投）—— 否决。
生产侧无从知道写线程何时就绪，要加新的握手状态，把简单问题复杂化。

**③ 只在 `finish_recording` 排空** —— 不够。
`_recording=True` 是同步的，`start_recording` 之后到写线程起来之间仍有几百 ms
的窗口在累积（2.3 的第 2 类）。**必须在 `_start_async` 里、写线程启动之前排空。**

**④ 最终采用：新增 `_drain_external_queues()`，在三处录制边界调用。**

### 2.5 最终方案

```python
# core/pipeline.py:294
def _drain_external_queues(self):
    """排空外部帧源队列（丢弃未消费的旧帧）。"""
    for q in self._external_queues.values():
        while not q.empty():
            try:
                q.get_nowait()
            except queue.Empty:
                break
```

三处调用：

| 位置 | 行 | 作用 |
|---|---|---|
| `_start_async` | `core/pipeline.py:588` | **主修复**，必须在 `_write_thread.start()` 之前 |
| `finish_recording` | `core/pipeline.py:962` | 写线程已停，立即释放 ~110 MB/槽 |
| `abort_recording` | `core/pipeline.py:1000` | 同上 |

对 IMU **无副作用**：被排空帧的 IMU 样本就在它自己的 tuple 里，本就是录制前的
样本，一同丢弃才正确；`_pending_imu` 防丢缓冲也已在更早的步骤清空。

`core/pipeline.py` 是共享模块 → 改完必须 `pack_lite --force` 刷新
`lite_package/`，已执行。

### 2.6 验证

**回归测试** `tools/tests/test_rgb_prestart_frames.py` 走**真实**
`start_recording → _start_async` 路径：

- monkeypatch `core.pipeline.EgoDataWriter` 换成 FakeWriter，
- FakeWriter 的 `start_episode` 由 `threading.Event` 闸门拉住，把启动窗口
  放大到可控；
- 帧的序号编码进像素，逐帧断言。

⚠️ **注意**：`tools/tests/test_imu_pending.py:79` 是直接置 `_recording = True`
来绕过启动路径的，**测不出这个 bug**。新测试必须走真实的异步启动路径，否则
`_start_async` 里的排空根本不会被执行。

结果：

- 正向：12 项断言全过（启动窗口内 70 帧 + 残留 5 帧全部丢弃，之后的真实帧 4/4 落盘）
- **反向验证**：去掉三处调用 → 8 项断言失败，且**视频首帧变成残留帧标记**、
  开头混入 5 帧残留 + 6 帧启动窗口帧——**精确复现用户症状**

**真机验收工具** `tools/check_segment_seam.py`（从任意 cwd 可运行，默认路径按
仓库根解析）。判据是**相对**的——相邻两段常在同一个桌面开录，首帧本来就可能
相似，所以看的是「首帧 vs 上段末帧」是否显著**低于**「首帧 vs 本段自己的倒数
第 30 帧」：

```
修复前的数据（最后 11 段全部命中）：
  episode-048.mp4   首帧vs上段末帧 4.83   vs自己倒数30帧 13.07   ⚠ 疑似残留
  episode-050.mp4                17.61                  61.28   ⚠ 疑似残留
  episode-056.mp4                 9.45                  46.70   ⚠ 疑似残留
FAIL: 11 段疑似以「录制开始前」的帧开头
```

重启主程序后再录一段，同一命令应变成 `PASS: 未发现段首继承上一段末尾的迹象`。

### 2.7 遗留：RGB 相对其他模态的恒定滞后（**尚未处理，待定**）

修 bug 时顺带量出来的第二个问题。用 `hardware_ns` 步长测 8 个 episode：

| episode | 行数 | hw 步长 | 行间隔 | → 生产者帧率 |
|---|---|---|---|---|
| 049 | 273 | 32.64 ms | 33.33 ms | 30.64 fps |
| 052 | 326 | 32.12 ms | 33.33 ms | 31.13 fps |
| 056 | 335 | 32.33 ms | 33.33 ms | 30.93 fps |

**RGB 生产者实际跑 30.5~31.1 fps（均值 30.84），而写线程恒定 30.0 fps**，
净盈余 ~2.8%。这部分**必须丢**（输出就是 30 fps），差别只在丢在哪：

- **现状**（drop-newest，队列满则丢弃本帧）：队列长期贴 maxsize 30
  → 录下的 RGB 画面**恒定落后行时间戳约 1.0 s**（30 帧 ÷ 30 fps），
  相对 tactile / SLAM / parquet 存在固定时间偏移。
- **修复后**：段首从 0 开始，但按 0.84 fps 回涨 → 12 s 的段末约 0.33 s，
  ~36 s 后重新填满。

**建议（未实施）**：把外部队列 `maxsize` 从 30 降到 ~4（`core/pipeline.py:284`）。
滞后降到 ≤0.13 s；**丢帧总数不变**（由盈余决定，不是队列决定的），但由「满了
突发丢一批」变成「均匀铺开」；代价是瞬态卡顿的吸收变浅。

---

## 三、两条链路的对照

两个问题共享同一个模式：

1. **症状出现在消费侧，但第一直觉总指向生产侧**。轨迹不光滑 → 怀疑 SLAM 出点慢；
   视频开头不对 → 怀疑相机多投。两次实测都证明生产侧是干净的。
2. **破案靠的是「量化不变量」而不是读代码**。轨迹这条是「位姿间隔恒 33.34 ms」；
   视频这条是「mp4 帧数 == parquet 行数」+「跨段末帧匹配」。先证明数据没错，
   才能把搜索范围缩到消费侧。
3. **根因都是一处「漏了对称分支」的边界处理**：`_decimated_trajectory` 的 stride
   翻倍让顶点数永远够不到上限（漏了「到达上限后应保持」）；清空逻辑排空了两条
   队列却漏了第三条。
4. **都必须有反向验证的回归测试**：`check_decimation` 把上限改回 400 必失败；
   新测试去掉三处排空必失败、且复现原始症状。只在正向通过的测试不算数。

---

## 四、RGB 帧空洞（v1.3.10）

2026-09-18

### 4.1 现象与「空洞」的口径

用户报：`episode-099.mp4` 开头约 40 帧**静止不动**，像是「刚开始录制的视频流没存下来」。
实测两件事：

- 开头那 45 帧**是真帧**：场景本身静止（帧间差 mad 0.45 = 传感器噪声，不是编码器
  重复帧的 0），且正好从按下录制那一刻开始 —— 老 bug（段首混入上一段残留帧）没有复发。
- 真正的缺陷是 **row44→row45 之间丢了 4.68 秒**：`hardware_ns` +4680.8ms、
  `wall_time` +4666.4ms 同步跳。mp4 是定 30fps 容器，500 帧只播 16.67s，实际跨
  21.32s ⇒ 播放表现为「静止 1.47s → 已经握在手里」，丢的正是操作者拿起夹爪那一段。
- 当时**全部计数器都是 0**（队列丢帧、重连、SLAM 丢帧……），日志里也没有一行异常
  —— 事后翻遍日志也判断不出是相机侧没帧可读、emit 帧槽被覆盖，还是 GUI 主线程卡顿：
  这三条路在 parquet 里留下**完全相同的空洞签名**。

口径（`core/frame_gap.GapWatch`，录制侧与离线审计**共用同一个类**，避免口径漂移）：

```
空洞 = 相邻两帧时间差 − 标称帧间隔（30fps → 33.3ms）
```

只报「断了多久」，不判「丢了几帧」（帧率本就不恒定，反推帧数会给出看似精确实则错的
数字）。日志里 `[录制] RGB 帧空洞 4648ms` 报的是**空洞**不是间隔（4680.8 − 33.3）。

### 4.2 两个戳分别是谁盖的（本节最要紧的一条）

| parquet 列 | 谁盖的 | 量的是 |
|---|---|---|
| `hardware_ns` | **采集线程**，相机 `read()` 返回处（`bridge._rgb_run` 的 `time.monotonic_ns()`） | **取到帧的时刻**（宿主单调钟；帧晚到、读侧停顿都记在这里） |
| `wall_time` | **写入线程**，建行落盘处（`egodata_writer.py` 的 `time.time()`） | **写下来的时刻**（宿主墙钟） |

于是 **Δwall − Δhw = 中间那段队列的滞留变化量**（两个钟的固定偏移在差里抵消）。
2026-09-18 在全库上验过，`episode-011`：

| 行 | Δhw | Δwall | 偏移阶跃 | 读法 |
|---|---|---|---|---|
| row162 | 28.6ms | 167.2ms | **+138.7ms** | 写侧积压 139ms（帧没少，只是写晚） |
| row180 | 164.7ms | 33.5ms | **−131.3ms** | 读侧一段空窗把积压排空（帧晚到） |

两侧各停一次，**是两件事**，不是「同一个洞的两半」——这一点直接改掉了审计脚本早期的
一个设计（曾把相隔十几行的一墙一戳「配对」成一次事件，等于把两次停顿捏成一次、把账
记到错的桶里；该层已删除）。`episode-014 / 018 / 007` 是同一种形状（+220.7/−217.7、
+235.2/−224.8、+238.0/−228.5）。墙钟单独跳一下也可能是 NTP 校时，但本库的墙钟事件
都成对出现且互相抵消，不是校时。

**早年段的坑**：`trunc32` 段（≤078，74/95 段）的 `hardware_ns` 是同一个宿主单调钟被
截成**有符号 32 位**纳秒（±2^31，每 4.295 秒锯齿一次）。判空洞前必须先按 2^32 解卷；
反过来，对 64 位戳（≥079）也解卷会把真空洞折叠 —— ep-099 的 4680.8ms 会变成 385ms。
`tools/tests/test_audit_frame_gaps.py` 对这两种反向错误各有用例钉住。

### 4.3 判据：画面是硬证据，钟只用来定性

单看时钟只能得出「有一段时间没有帧」，**是不是画面上真断了一截，必须看画面**。
审计脚本把整段视频降采样成灰度，比较事件前后两帧的差（mad）与两个参照：

```
boundary = mad(第 row−1 帧, 第 row 帧)                  ← 事件处
near     = 同段 ±15 帧内相邻帧差的中位数                  ← 「正常一帧动多少」
sat      = 每隔 60 帧取一对的中位差                       ← 「这段视频能动到多大」
```

| 判据 | 结论 |
|---|---|
| `boundary ≤ 2.0 × near` | **画面连续** ⇒ 这一起不是丢帧 |
| `boundary ≥ 3.0 × near` 或 `≥ 0.7 × sat` | **画面跳变** ⇒ 丢了 |
| 介于两者之间 | **不定** ⇒ 回落钟口径，进「待复核」 |
| `near` 与 `boundary` 都 < 1.5（8 位灰度噪声地板，静止场景实测 0.45） | **不定** —— 静止画面里丢一段也看不出来，**不许反着判「没丢」** |

标定（2026-09-18）：真丢的两处 `ep-099 row45 = 18.6×`、`ep-098 row40 = 91.6×`
（对饱和 0.75 / 1.62×）；而 15 个「双钟都跳 ~250ms」的事件全是 0.16~1.60×，
即 boundary **正好是一帧的运动量**（钟却说过了 8 帧）——它们不是丢帧。
`diag_frame_trace.py` 那条经验同样适用：**判据必须带同窗对照，裸 mad 会判错**。

### 4.4 全库基线（95 段，2026-09-18）

```
venv/bin/python tools/audit_frame_gaps.py        # 只读；有画面证实丢帧时退出码 1
```

| 归账 | 段数 | 累计 | 含义 |
|---|---|---|---|
| **画面证实丢了** | **25** | **37412.6ms** | 画面真断了一截（最大 4647.5ms @ 段099） |
| 钟跳但画面连续 | 72 | 20076.2ms | 有停顿、画面接得上（帧到得晚 / 写得晚） |
| 写侧打嗝 | 43 | 9944.8ms | 墙钟洞而戳只走一帧 ⇒ 帧没少、只是写晚 |
| 待复核 | 0 | 0ms | 画面判不了或未复核 |

- 25 起丢帧 = **23 段早年段（006~055）+ 2 段（098 / 099）**，**全部落在开录后
  0.13~1.47s**；其中 18 起 ≥1s、7 起 <1s。这不是随机掉帧，是启动窗口的问题。
- 画面复核实际覆盖 85 段（有事件的段才解码；95 段的 mp4 帧数与 parquet 行数**全部一致**）。
- 与「只按钟算」的差别：钟口径会把「戳跳了但画面是连的」也算成丢（那 20.1s 全是这
  一类），又会把早年段启动窗口里只有戳跳、没有墙钟跳的洞判成「判不了」。**数字变了
  是因为口径从「钟说跳了」换成了「画面真断了」** —— 后者才是用户看得见、也才是
  数据集真正损失的那部分。

### 4.5 为什么全落在启动窗口（机制，数字自洽）

2.7 节量过：RGB 生产者实际跑 30.84fps（30.5~31.1），写线程恒定 30.0fps ⇒ 外部队列
（`maxsize=30`）按 **~0.84 帧/秒**回涨。于是：

- **段首队列是空的**：头一两秒只有 1~2 帧余量，一次停顿就直接落进画面（25 起全在
  1.47s 之内）。
- **队列越深越能垫**：满仓 30 帧 = 1s 余量。后期最大的读侧停顿是
  `ep-018 row291 = 257.8ms`（开录后 9.9s），而那时队列只积了 ~8 帧（0.27s）——
  **刚好够**；写侧停顿（最长 `ep-014 row275 = 798.5ms`）根本不丢内容，队列涨一下
  就吸收了。
- 代价是 2.7 节那条恒定 ~1s 滞后 —— 队列深度换的就是抗顿挫能力。

这是**推测**（每一环都有数，但没有一条日志能直接证明「那一刻队列里有几帧」）。真机
确认靠 4.6 的四层仪表：`readfail_max_ms ≈ gap_max_ms` 且 `reconnect_count ≥ 1` ⇒
相机/服务侧停摆；`readfail ≈ 0` 而 `overwrite_count` 大 ⇒ emit 侧没取走。

### 4.6 本次做了什么（只装仪表 + 留档）

用户拍板：**本次只让下次复现时日志直接指认是哪一层，不改录制通路行为**。
（明确不做：录制通路与显示解耦、相机侧自愈、外部队列 `maxsize` 改小。）

| 层 | 指标（parquet `drop_stats` 的键） | 实时告警 |
|---|---|---|
| 落盘侧 | `<slot>_gap_ms` / `_gap_max_ms` / `_gap_count` | `[录制] RGB 帧空洞 …ms（槽 …，开录后 …s 起）` |
| 采集侧 | `<slot>_readfail_ms` / `_readfail_max_ms` / `_readfail_stall_count` / `_reconnect_count` | `[夹爪-RGB告警] RGB 采集停摆 …s` / `…已恢复`（进入一次 / 30s 限频 / 恢复一次） |
| emit 与派发 | `<slot>_overwrite_count` / `_emit_lag_max_ms` / `_dispatch_lag_max_ms` | 录制结束汇总行（值 >0 才打） |
| camera-service | `logs/camera_service/<时刻>_<tag>_camera-service.log`（留最近 20 份）+ 告警摘录进 `logs/main.log` | 摘录只在有告警时打 |

键的约定（`core/pipeline.is_frame_drop_key`）：**新键全带 `_ms`/`_count` 后缀**，
不计进「丢帧统计」的帧数里（`[录制] 丢帧统计` 与「换编码器」提示一个字没改）。
后缀是硬约束，不是风格：采集侧读数里有几个键（停摆次数曾叫 `readfail_episodes`）
不在后缀表里，会**原样被当成帧数加进「丢帧统计」**——正是本次要消灭的那类混淆。
现在 `test_gripper_bridge` 逐个键走一遍 `rgb_quality_snapshot()`，`test_drop_stats`
的真值表覆盖全部 10 个会落盘的诊断键。
留档与摘录失败一律只记一行日志，绝不打断清理流程；**不含体积上限**（每 5 秒一行
stats，量本就极小），与 `fays_single.py` 的 slam_native 留档逐字同构。

### 4.7 回归与验收

```bash
# 离线自检（都不需要硬件；rc=0 且要看得到 PASS 行）
QT_QPA_PLATFORM=offscreen venv/bin/python tools/tests/test_frame_gap.py
QT_QPA_PLATFORM=offscreen venv/bin/python tools/tests/test_rgb_quality.py
QT_QPA_PLATFORM=offscreen venv/bin/python tools/tests/test_ext_frame_gap.py
QT_QPA_PLATFORM=offscreen venv/bin/python tools/tests/test_camera_log_archive.py
QT_QPA_PLATFORM=offscreen venv/bin/python tools/tests/test_audit_frame_gaps.py
# 全库审计（只读）
venv/bin/python tools/audit_frame_gaps.py
```

真机验收（录音 30s+ 后看）：① `[夹爪-RGB告警]` 与恢复行；② `[录制] RGB 帧空洞 …ms`；
③ `logs/main.log` 里的 `[UVC] camera-service 告警摘录` + 留档路径；
④ `logs/camera_service/` 下有 `<时刻>_bus*-port*_camera-service.log`；
⑤ parquet 的 `drop_stats` 含 `*_gap_*`，且 `[录制] 丢帧统计` 的帧数里**没有**它们。

**遗留**：2.7 节那条「外部队列 maxsize 30 → ~4」的建议**仍未实施**（滞后 1s → 0.13s，
但抗顿挫变浅）；本次不动录制时序，留给后续按真机证据决策。
