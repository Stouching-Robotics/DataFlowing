# -*- coding: utf-8 -*-
# PEP 263 编码声明：下方的中文注释是 UTF-8。
# 极简采集版依赖白名单（start_lite.bat / start_lite.sh 使用）
# 只保留: 连接设备 → 采集(parquet+MP4) → 上传。
# 不含回放/登录/任务页/骨架解算(toolkit/scipy/loguru/pydantic/polars)/pyvista 等重型依赖。
# 注意: imageio-ffmpeg(约 77MB) 提供静态 ffmpeg 二进制(含 x264)，
#       客户机无 PATH ffmpeg 时录制链必需；装有系统 ffmpeg 的机器可删掉本行。
# UVC 摄像头零新增依赖: Linux 走 sysfs 枚举；Windows 无 pygrabber 时
#       自动退化 DShow 索引探测（装了 pygrabber+comtypes 仅提升枚举质量）。
PyQt5>=5.15.0
numpy>=1.24
opencv-python>=4.10
pyarrow>=14.0.0
imageio-ffmpeg>=0.5.0
bleak>=0.20.0
requests>=2.28
pyserial>=3.5
pyrealsense2
